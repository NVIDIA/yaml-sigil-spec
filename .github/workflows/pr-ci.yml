name: Protected pull request CI

on:
  workflow_call:
    inputs:
      pull_number:
        description: Pull request number.
        required: true
        type: string
      head_sha:
        description: Exact authorized pull request head.
        required: true
        type: string
      base_sha:
        description: Exact protected-main base.
        required: true
        type: string
      policy_sha:
        description: Exact protected-main policy commit.
        required: true
        type: string
      comment_id:
        description: Exact authorization comment.
        required: true
        type: string
      binding_digest:
        description: Exact full authorization binding digest.
        required: true
        type: string
    outputs:
      results_json:
        description: Canonical complete protected-job result inventory.
        value: ${{ jobs.results.outputs.results_json }}

permissions: {}

concurrency:
  group: protected-pr-ci-${{ github.repository }}-${{ inputs.pull_number }}
  cancel-in-progress: false

jobs:
  authorize:
    name: "protected-ci|pr=${{ inputs.pull_number }}|head=${{ inputs.head_sha }}|comment=${{ inputs.comment_id }}"
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      actions: read
      contents: read
      issues: read
      pull-requests: read
    outputs:
      repository: ${{ steps.authorize.outputs.repository }}
      pull_number: ${{ steps.authorize.outputs.pull_number }}
      head_sha: ${{ steps.authorize.outputs.head_sha }}
      base_sha: ${{ steps.authorize.outputs.base_sha }}
      head_repository: ${{ steps.authorize.outputs.head_repository }}
      policy_sha: ${{ steps.authorize.outputs.policy_sha }}
      comment_id: ${{ steps.authorize.outputs.comment_id }}
    steps:
      # Authorization remains checkout-free and reloads the immutable policy
      # selected by the protected issue-comment caller.
      - name: Load protected authorization policy
        env:
          GH_TOKEN: ${{ github.token }}
          POLICY_SHA: ${{ github.sha }}
        run: |
          install -d "${RUNNER_TEMP}/protected-pr-ci"
          gh api \
            "repos/${GITHUB_REPOSITORY}/contents/.github/scripts/protected_pr_ci.py?ref=${POLICY_SHA}" \
            --jq .content | base64 --decode > "${RUNNER_TEMP}/protected-pr-ci/controller.py"
          gh api \
            "repos/${GITHUB_REPOSITORY}/contents/.github/protected-pr-ci.json?ref=${POLICY_SHA}" \
            --jq .content | base64 --decode > "${RUNNER_TEMP}/protected-pr-ci/policy.json"
          test -s "${RUNNER_TEMP}/protected-pr-ci/controller.py"
          test -s "${RUNNER_TEMP}/protected-pr-ci/policy.json"
          python3 -m py_compile "${RUNNER_TEMP}/protected-pr-ci/controller.py"

      - name: Reauthorize called command and candidate
        id: authorize
        env:
          GITHUB_TOKEN: ${{ github.token }}
          POLICY_SHA: ${{ github.sha }}
        run: >-
          python3 "${RUNNER_TEMP}/protected-pr-ci/controller.py" authorize-call
          --event "${GITHUB_EVENT_PATH}"
          --config "${RUNNER_TEMP}/protected-pr-ci/policy.json"
          --github-output "${GITHUB_OUTPUT}"
          --repository "${GITHUB_REPOSITORY}"
          --pull-number "${{ inputs.pull_number }}"
          --head-sha "${{ inputs.head_sha }}"
          --base-sha "${{ inputs.base_sha }}"
          --policy-sha "${{ inputs.policy_sha }}"
          --binding-digest "${{ inputs.binding_digest }}"
          --comment-id "${{ inputs.comment_id }}"
          --run-id "${GITHUB_RUN_ID}"
          --run-attempt "${GITHUB_RUN_ATTEMPT}"

  commit_policy:
    name: Commit policy
    needs: authorize
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - name: Check out protected policy
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 1
          path: policy
          persist-credentials: false
          ref: ${{ needs.authorize.outputs.policy_sha }}

      - name: Check out and verify exact candidate
        uses: ./policy/.github/actions/protected-candidate-checkout
        with:
          base-sha: ${{ needs.authorize.outputs.base_sha }}
          candidate-path: candidate
          fetch-depth: 0
          head-sha: ${{ needs.authorize.outputs.head_sha }}
          policy-path: policy
          repository: ${{ needs.authorize.outputs.head_repository }}

      # The fork checkout may not contain the base object. Fetch only the exact
      # public base SHA before running the byte-identical protected helper.
      - name: Load exact base commit
        env:
          BASE_SHA: ${{ needs.authorize.outputs.base_sha }}
        run: >-
          git -C candidate fetch --no-tags --no-recurse-submodules --depth=1
          "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git" "${BASE_SHA}"

      - name: Check pull request commits
        env:
          BASE_SHA: ${{ needs.authorize.outputs.base_sha }}
          HEAD_SHA: ${{ needs.authorize.outputs.head_sha }}
        run: ../policy/.github/scripts/check-pull-request-commits.sh
        working-directory: candidate

  workflow_lint:
    name: GitHub Actions
    needs: authorize
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - name: Check out protected policy
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 1
          path: policy
          persist-credentials: false
          ref: ${{ needs.authorize.outputs.policy_sha }}

      - name: Check out and verify exact candidate
        uses: ./policy/.github/actions/protected-candidate-checkout
        with:
          base-sha: ${{ needs.authorize.outputs.base_sha }}
          candidate-path: candidate
          fetch-depth: 1
          head-sha: ${{ needs.authorize.outputs.head_sha }}
          policy-path: policy
          repository: ${{ needs.authorize.outputs.head_repository }}

      # actionlint discovers configuration and workflows at the workspace root.
      # Stage candidate YAML as inert input beside protected-main configuration.
      - name: Stage workflows for linting
        run: |
          # The staged root is deliberately checkout-free. Initialize only Git
          # metadata so actionlint recognizes it as a project workspace.
          git init --quiet --initial-branch=main .
          install -d .github/workflows
          install -m 0644 policy/.github/actionlint.yaml .github/actionlint.yaml
          find candidate/.github/workflows -maxdepth 1 -type f \
            \( -name '*.yml' -o -name '*.yaml' \) \
            -exec install -m 0644 '{}' .github/workflows/ \;
          test "$(find .github/workflows -maxdepth 1 -type f | wc -l)" -gt 0

      - name: Lint GitHub Actions workflows
        uses: devops-actions/actionlint@9fb9c192862f19e684586ca84b500461bcd8a541 # v0.1.12 (actionlint v1.7.12)

      # This section would benefit by a change to the action allowlist
      - name: Lint candidate shell scripts
        uses: ludeeus/action-shellcheck@00cae500b08a931fb5698e11e79bfbd38e612a38 # 2.0.0
        with:
          check_together: true
          scandir: candidate/.github/scripts
          version: v0.11.0

      - name: Compile and test protected controller
        run: |
          python3 -m py_compile \
            policy/.github/scripts/protected_pr_ci.py \
            policy/.github/scripts/test_protected_pr_ci.py
          python3 -m unittest discover \
            -s policy/.github/scripts -p 'test_protected_pr_ci.py'

  platform_verifier:
    name: Protected checkout verifier (${{ matrix.name }})
    needs: authorize
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 15
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Linux
            runner: ubuntu-latest
          - name: macOS
            runner: macos-latest
          - name: Windows
            runner: windows-latest
    steps:
      # These regressions execute only protected policy. Candidate conformance
      # remains in the separate Linux terminal job below.
      - name: Check out protected policy
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 1
          path: policy
          persist-credentials: false
          ref: ${{ needs.authorize.outputs.policy_sha }}

      - name: Compile and test protected checkout verifier
        shell: bash
        run: |
          python3 -m py_compile \
            policy/.github/scripts/protected_checkout.py \
            policy/.github/scripts/protected_pr_ci.py \
            policy/.github/scripts/test_protected_pr_ci.py
          python3 -m unittest discover \
            -s policy/.github/scripts -p 'test_protected_pr_ci.py'

  tool_isolation:
    name: Trusted tool isolation
    needs: authorize
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    env:
      ACTIONS_CACHE_URL: ""
      ACTIONS_ID_TOKEN_REQUEST_TOKEN: ""
      ACTIONS_ID_TOKEN_REQUEST_URL: ""
      ACTIONS_RESULTS_URL: ""
      ACTIONS_RUNTIME_TOKEN: ""
      GH_TOKEN: ""
      GITHUB_TOKEN: ""
    steps:
      - name: Check out protected policy
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 1
          path: policy
          persist-credentials: false
          ref: ${{ needs.authorize.outputs.policy_sha }}

      # Resolve the supported runner's trusted executables before candidate
      # files exist, then prove a checkout-root decoy cannot replace them.
      - name: Resolve trusted executable paths
        id: trusted
        run: |
          cargo_path="$(command -v cargo)"
          git_path="$(command -v git)"
          jq_path="$(command -v jq)"
          python_path="$(command -v python3)"
          test "${cargo_path#/}" != "${cargo_path}"
          test "${git_path#/}" != "${git_path}"
          test "${jq_path#/}" != "${jq_path}"
          test "${python_path#/}" != "${python_path}"
          test -x "${cargo_path}"
          test -x "${git_path}"
          test -x "${jq_path}"
          test -x "${python_path}"
          {
            printf 'cargo=%s\n' "${cargo_path}"
            printf 'git=%s\n' "${git_path}"
            printf 'jq=%s\n' "${jq_path}"
            printf 'python=%s\n' "${python_path}"
            printf 'path=%s\n' "${PATH}"
          } >> "${GITHUB_OUTPUT}"

      - name: Check out and verify exact candidate
        uses: ./policy/.github/actions/protected-candidate-checkout
        with:
          base-sha: ${{ needs.authorize.outputs.base_sha }}
          candidate-path: candidate
          fetch-depth: 1
          head-sha: ${{ needs.authorize.outputs.head_sha }}
          policy-path: policy
          repository: ${{ needs.authorize.outputs.head_repository }}

      - name: Reject candidate executable shadowing
        env:
          TRUSTED_CARGO: ${{ steps.trusted.outputs.cargo }}
          TRUSTED_GIT: ${{ steps.trusted.outputs.git }}
          TRUSTED_JQ: ${{ steps.trusted.outputs.jq }}
          TRUSTED_PATH: ${{ steps.trusted.outputs.path }}
          TRUSTED_PYTHON: ${{ steps.trusted.outputs.python }}
        run: |
          probe_tool() {
            local tool="$1"
            local expected="$2"
            local decoy="candidate/${tool}"
            test ! -e "${decoy}"
            install -m 0755 /bin/false "${decoy}"
            local resolved
            resolved="$(cd candidate && PATH="${TRUSTED_PATH}" command -v "${tool}")"
            rm "${decoy}"
            if [[ "${resolved}" != "${expected}" ]]; then
              printf '%s resolved to %s instead of protected path %s\n' \
                "${tool}" "${resolved}" "${expected}" >&2
              return 1
            fi
          }

          probe_tool cargo "${TRUSTED_CARGO}"
          probe_tool git "${TRUSTED_GIT}"
          probe_tool jq "${TRUSTED_JQ}"
          probe_tool python3 "${TRUSTED_PYTHON}"

  markdown:
    name: Markdown
    needs: authorize
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - name: Check out protected policy
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 1
          path: policy
          persist-credentials: false
          ref: ${{ needs.authorize.outputs.policy_sha }}

      - name: Check out and verify exact candidate
        uses: ./policy/.github/actions/protected-candidate-checkout
        with:
          base-sha: ${{ needs.authorize.outputs.base_sha }}
          candidate-path: candidate
          fetch-depth: 1
          head-sha: ${{ needs.authorize.outputs.head_sha }}
          policy-path: policy
          repository: ${{ needs.authorize.outputs.head_repository }}

      - name: Lint Markdown with protected configuration
        # Refresh this immutable pin frequently to keep pace with rumdl releases.
        uses: rvben/rumdl@95a982d941d00cf41a0dcef4d0788bd15a29be1b # v0.2.54
        with:
          config: policy/.rumdl.toml
          path: candidate
          version: "0.2.54"

  contracts:
    name: Schema and Protobuf
    needs: authorize
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - name: Check out protected policy
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 1
          path: policy
          persist-credentials: false
          ref: ${{ needs.authorize.outputs.policy_sha }}

      - name: Check out and verify exact candidate
        uses: ./policy/.github/actions/protected-candidate-checkout
        with:
          base-sha: ${{ needs.authorize.outputs.base_sha }}
          candidate-path: candidate
          fetch-depth: 1
          head-sha: ${{ needs.authorize.outputs.head_sha }}
          policy-path: policy
          repository: ${{ needs.authorize.outputs.head_repository }}

      - name: Check Protobuf
        uses: bufbuild/buf-action@8c6a16e16f12ba20b6470afa9c2ba9b5ba8c97c3 # v1.5.0
        with:
          archive: false
          breaking: false
          format: true
          input: candidate/proto
          lint: true
          pr_comment: false
          push: false

      - name: Validate JSON Schema
        run: jq empty candidate/schema/YamlSigilSignature.v1alpha1.schema.json

  rebuild_rs:
    name: Rust conformance generator
    needs: authorize
    runs-on: ubuntu-latest
    timeout-minutes: 120
    permissions:
      contents: read
    steps:
      # ACVP input is bounded and the validator is built from protected policy
      # before candidate Rust executes under a distinct terminal OS identity.
      # No repository-backed Action or post-hook exists in this job.
      - name: Run terminal protected conformance validation
        env:
          BASE_SHA: ${{ needs.authorize.outputs.base_sha }}
          GITHUB_TOKEN: ${{ github.token }}
          HEAD_REPOSITORY: ${{ needs.authorize.outputs.head_repository }}
          HEAD_SHA: ${{ needs.authorize.outputs.head_sha }}
          POLICY_SHA: ${{ needs.authorize.outputs.policy_sha }}
        shell: bash
        run: |
          set -euo pipefail
          policy_root="$(mktemp -d)"
          chmod 0755 "${policy_root}"
          git -c core.autocrlf=false -c credential.helper= \
            init --quiet --initial-branch=main "${policy_root}"
          git -C "${policy_root}" remote add origin \
            "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git"
          GIT_TERMINAL_PROMPT=0 git -c core.autocrlf=false \
            -c credential.helper= -C "${policy_root}" fetch --no-tags \
            --no-recurse-submodules --depth=1 origin "${POLICY_SHA}"
          git -C "${policy_root}" checkout --quiet --detach FETCH_HEAD
          exec bash --noprofile --norc \
            "${policy_root}/.github/scripts/run-terminal-candidate.sh" \
            protected-ci "${RUNNER_OS}" "${policy_root}" "${POLICY_SHA}" \
            "${GITHUB_REPOSITORY}" "${HEAD_REPOSITORY}" \
            "${BASE_SHA}" "${HEAD_SHA}" "${GITHUB_ENV}"

  results:
    name: Collect protected job results
    if: ${{ always() }}
    needs:
      - authorize
      - commit_policy
      - workflow_lint
      - platform_verifier
      - tool_isolation
      - markdown
      - contracts
      - rebuild_rs
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions: {}
    outputs:
      results_json: ${{ steps.results.outputs.results_json }}
    steps:
      # Only fixed job-result enums enter this closed, complete JSON inventory.
      - name: Expose exact candidate results to the protected caller
        id: results
        env:
          COMMIT_POLICY_RESULT: ${{ needs.commit_policy.result }}
          CONTRACTS_RESULT: ${{ needs.contracts.result }}
          MARKDOWN_RESULT: ${{ needs.markdown.result }}
          PLATFORM_VERIFIER_RESULT: ${{ needs.platform_verifier.result }}
          REBUILD_RS_RESULT: ${{ needs.rebuild_rs.result }}
          TOOL_ISOLATION_RESULT: ${{ needs.tool_isolation.result }}
          WORKFLOW_LINT_RESULT: ${{ needs.workflow_lint.result }}
        run: >-
          printf 'results_json={"commit_policy":"%s","workflow_lint":"%s","platform_verifier":"%s","tool_isolation":"%s","markdown":"%s","contracts":"%s","rebuild_rs":"%s"}\n'
          "${COMMIT_POLICY_RESULT}" "${WORKFLOW_LINT_RESULT}"
          "${PLATFORM_VERIFIER_RESULT}" "${TOOL_ISOLATION_RESULT}"
          "${MARKDOWN_RESULT}"
          "${CONTRACTS_RESULT}" "${REBUILD_RS_RESULT}"
          >> "${GITHUB_OUTPUT}"

name: Trusted CI

# Only ci.yml selects this route. Local calls use the exact caller commit;
# ordinary copied-ref reporting binds this file to protected main as well.
on:
  workflow_call:

permissions: {}

# Workflow-level caller env is not inherited by local reusable workflows.
env:
  BUF_TOOLCHAIN_VERSION_REQUIREMENT: ">=1.73.0"

jobs:
  trusted_ci:
    name: Trusted CI (Linux)
    runs-on: linux-amd64-cpu8
    timeout-minutes: 120
    permissions:
      contents: read
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Lint GitHub Actions workflows
        uses: devops-actions/actionlint@ec02b36684b2f574f1d219ad0a43b082e46bf3e4 # v0.1.13 (actionlint v1.7.12)

      # This section would benefit by a change to the action allowlist
      - name: Lint shell scripts
        uses: ludeeus/action-shellcheck@00cae500b08a931fb5698e11e79bfbd38e612a38 # 2.0.0
        with:
          check_together: true
          scandir: .github/scripts
          version: v0.11.0

      - name: Test CI helpers
        run: |
          .github/scripts/check-trusted-tool-pins.sh
          .github/scripts/test-trusted-tool-pins.sh
          python3 -m py_compile \
            .github/scripts/bind-candidate-pr.py \
            .github/scripts/report_required_ci.py \
            .github/scripts/test_bind_candidate_pr.py \
            .github/scripts/test_report_required_ci.py
          python3 -m unittest discover \
            -s .github/scripts -p 'test_*.py'
          .github/scripts/test-materialize-candidate.sh

      - name: Lint Markdown
        # Refresh this immutable pin frequently to keep pace with rumdl releases.
        # This section would benefit by a change to the action allowlist
        uses: rvben/rumdl@95a982d941d00cf41a0dcef4d0788bd15a29be1b # v0.2.54
        with:
          version: "0.2.75"

      - name: Set up Rust
        uses: actions-rust-lang/setup-rust-toolchain@166cdcfd11aee3cb47222f9ddb555ce30ddb9659 # v1.17.0
        with:
          cache: false
          components: clippy, rustfmt
          toolchain: 1.98.0

      - name: Install Buf
        run: |
          cargo install --locked --version "${BUF_TOOLCHAIN_VERSION_REQUIREMENT}" buf-toolchain
          BUF_RS_VALIDATE_OFFLINE=1 "${CARGO_HOME:-${HOME}/.cargo}/bin/validate-cargo-buf-toolchain"

      - name: Check Protobuf
        run: |
          BUF="${CARGO_HOME:-${HOME}/.cargo}/bin/buf" \
            cargo xtask check --only=buf-build,buf-lint,buf-fmt

      - name: Validate JSON Schema
        run: cargo xtask check --only=schema

      - name: Install Rust validation tools
        uses: taiki-e/install-action@9114bf4d891761788c546334fd37538eae1bf8b3 # v2.87.16
        with:
          checksum: true
          fallback: none
          tool: cargo-audit@0.22.2,cargo-deny@0.20.2,cargo-machete@0.9.2

      - name: Verify exact trusted validation tools
        run: |
          rustc --version --verbose | grep -Fx 'release: 1.98.0'
          test "$(cargo-audit --version)" = 'cargo-audit 0.22.2'
          test "$(cargo-deny --version)" = 'cargo-deny 0.20.2'

      - name: Cache trusted Rust build inputs
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          cache-bin: false
          # Retain source registry data only; target/ may contain executables.
          cache-targets: false
          save-if: ${{ github.ref == 'refs/heads/main' }}

      - name: Check Rust formatting
        run: cargo xtask check --only=fmt

      - name: Check Rust
        run: cargo xtask check --only=check

      - name: Lint Rust
        run: cargo xtask check --only=clippy

      - name: Test Rust
        run: cargo xtask check --only=test

      - name: Check unused Rust dependencies
        run: cargo xtask check --only=machete

      - name: Check Rust dependency policy
        run: cargo xtask check --only=deny

      - name: Audit Rust dependencies
        run: cargo xtask check --only=audit


  trusted_result:
    name: Linux result
    if: ${{ always() }}
    needs:
      - trusted_ci
    runs-on: linux-amd64-cpu4
    timeout-minutes: 5
    permissions: {}
    steps:
      # This isolated aggregate consumes only GitHub job results. Missing,
      # skipped, cancelled, or failed authoritative lanes cannot pass staging.
      - name: Require every authoritative Linux lane
        env:
          TRUSTED_CI_RESULT: ${{ needs.trusted_ci.result }}
        run: |
          test "${TRUSTED_CI_RESULT}" = success
